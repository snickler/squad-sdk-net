---
"Squad.SDK.NET": minor
---

Align the repository with upstream's `dev -> preview -> main` model, add changeset-driven release preparation for the .NET SDK, and switch stable releases to automatic tagging and publishing from `main`.
